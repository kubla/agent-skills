# Local Dashboard Data

This directory is used by the `fulcra-skills-dashboard` skill to store local data fetches, JSON records, generated assets, and chat history. 

**This data is strictly ignored by version control to prevent exposing personal user data.**